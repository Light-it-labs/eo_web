const BASE = "/app/";
export const ROUTES = {
  base: `${BASE}home`,
  login: `${BASE}login`,
  register: `${BASE}register`,
  registrationComplete: `${BASE}register/complete`,
  home: `${BASE}home`,
  profiling1: `${BASE}profiling1`,

} as const;
